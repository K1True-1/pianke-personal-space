# 夜色画廊动效版检查记录

final result: blocked

日期：2026-09-12。当前源码已实现，尚未通过浏览器视觉验收，尚未发布。

## 阻塞与证据

- 内置浏览器连接返回 `nodeRepl.fetch request failed`，无法取得标签页或截图。
- 用户已明确允许 Playwright。Playwright 启动本机浏览器时返回 `spawn EPERM`；独立 Chrome / Edge 进程也因 Windows IPC / Mojo `Access is denied` 退出。没有修改系统安全策略或使用个人浏览器资料。
- 生产构建在加载配置时被 Wrangler / esbuild 子进程的 `spawn EPERM` 阻止，未完成生产构建。
- 本地 Node 设计预览已恢复：`http://127.0.0.1:5173/` 返回 200，真实页面 HTML、界面模块、样式、两张照片和森林光影素材均返回 200。HTTP 检查不能证明浏览器渲染、动画或交互正确。
- 此预览运行现有应用源码，以独立的本地预览配置绕开不可用的 Cloudflare 开发进程。匿名作品接口返回 200；Cloudflare 数据库、对象存储和真实登录不可用于此预览，不能据此宣称上传端到端测试通过。生产配置未被替换。

## 视觉目标与对比条件

- 选择：用户选定第二张「夜色画廊」，并要求增加动效及光影层次。
- source visual truth path: `C:/Users/110637/.codex/generated_images/01a08f09-982c-7d22-8e18-65008934d583/exec-7576bee1-7431-4f70-93d9-ce3fbc083f07.png`
- source pixels: 1487 × 1058。
- implementation screenshot path: 无；浏览器启动受阻。
- viewport: 待捕获桌面 1487 × 1058 CSS px，手机 390 × 844 CSS px，deviceScaleFactor 1。
- implementation pixels / density normalization: 未取得截图，未归一化。
- state: 相册默认页、两张展示样片、空个人作品和零活动记录。设计稿中的示意活动量、拍摄日期和地点不作为真实内容。
- full-view comparison evidence: 无；不能进行视觉匹配判断。
- focused region comparison evidence: 无；待检查标题与导航、照片边缘和文字、日历标签与日期单元格、弹窗。

## 必查视觉表面

| 表面 | 当前状态与待查项 |
| --- | --- |
| 字体与排版 | 未经浏览器核验。待核对中文实际回退字体、字重、标题换行、小号字可读性。 |
| 间距与布局 | 未经浏览器核验。待核对侧栏比例、照片高度、首屏内容、手机溢出与导航。 |
| 色彩与视觉样式 | 未经浏览器核验。待核对生成背景亮度、覆盖层、边缘光、按钮和文字对比度。 |
| 图像质量 | 真实照片和生成背景文件已保留，HTTP 可读取；裁切、清晰度及最终合成效果待截图核验。 |
| 文案与内容 | 代码检查确认样片明确标注来源，未复制设计稿虚构日期与活动记录；实际截断、换行和状态切换待查。 |

## 已完成的实现与代码检查

- 深色画廊布局、真实森林光影背景、照片微视差和边缘光、分类指示器过渡、作品入场、弹窗与按钮反馈。
- 手机布局与 `prefers-reduced-motion` 分支已实现；触摸设备不启用指针视差。小说正文没有位移动画。
- 保留既有照片 / 代码 / 小说分片上传、预览、阅读、下载、删除和真实 365 天活动数据逻辑。
- 修复代码审查发现的手机上传按钮名称、弹窗关闭焦点目标、日期选中后活动值刷新，以及入场动画遮盖悬停变换的问题。
- `tsc --noEmit` 在最终源码上通过。
- 变更文件 ESLint：2 个既有 `react-hooks/set-state-in-effect` 错误、2 个原生图片提示；未宣称 lint 通过。对比原版为 6 个错误、2 个图片提示。新动效组件无 lint 错误。
- 本地 HTTP 检查通过：页面、匿名作品接口、主页面模块、动效模块、样式、背景图、海岸和建筑样片。

## 对比历史

尚未完成第一次浏览器视觉对比。以上修复来自代码审查，不作为视觉 QA 迭代。不存在修复后的浏览器截图证据。

## 待执行清单

1. 在可启动浏览器的环境捕获桌面和手机截图，将参考图与同状态截图放进同一对比输入。
2. 检查照片悬停、离开和打开详情，分类切换，手机上传按钮，弹窗 Escape 与关闭后的焦点。
3. 检查日历键盘移动、横向滚动和活动更新反馈；检查系统减少动态效果及触摸设备行为。
4. 检查浏览器控制台、加载错误、页面横向溢出和所有必查视觉表面，修复 P0 / P1 / P2 后重新截图比较。
5. 在支持 Cloudflare 开发环境的机器完成生产构建与登录后上传 / 阅读回归。
6. 检查通过后更新本报告；用户要求发布时再发布，并移除 README 中的未发布状态说明。

当前没有可证实的视觉匹配结论，也没有可宣称通过的浏览器交互结果。
